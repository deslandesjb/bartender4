DEVICES=("4c-b9-9b-26-f2-21" "38-88-a4-cc-bb-b1")

IS_CONNECTED=0
for DEVICE in "${DEVICES[@]}"
do
    if [ "$(/opt/homebrew/bin/blueutil --is-connected $DEVICE)" = "1" ]; then
        IS_CONNECTED=1
        break
    fi
done

if [ "$IS_CONNECTED" = "1" ]; then
    echo "1"
else
    echo "0"
fi
